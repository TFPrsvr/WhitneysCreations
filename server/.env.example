# MongoDB Configuration
# IMPORTANT: Replace with YOUR credentials from MongoDB Atlas
# NEVER commit the actual .env file - this is just a template
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_NEW_PASSWORD@cluster0.xxxxx.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# Application Secret
# Generate a new random secret key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEY=YOUR_NEW_SECRET_KEY_HERE

# Server Configuration
PORT=3002
NODE_ENV=development

# SECURITY REMINDER:
# 1. Change your MongoDB password at https://cloud.mongodb.com
# 2. Generate a new SECRET_KEY using the command above
# 3. Copy this file to .env and fill in your actual credentials
# 4. NEVER commit the .env file to git
